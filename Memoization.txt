


function compute(n,k){
    return factorial(n)/(factorial(k) + factorial(n-k));
}

let blendC = function{
    lete memo = []
    return function inner (n,k){
        if (n > memo.length - 1){
            memo[n] = [];
            mem0[n][k] = comupute(n,k);
        }else if (n < memo.length && k > memo[n].length - 1){
            memo[n][k] = comupute(n,k);
        }
        return memo[n][k]
    }
}